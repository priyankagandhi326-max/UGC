# UGC

**Env:** env must live in `ugc/.env.local`. If you have an accidental `ugc/ugc/.env.local`, remove it.

## Start dev server

```bash
npm run dev
```

Run: `npm run dev` from repo root. It will auto-pick a port and open `/login`.
